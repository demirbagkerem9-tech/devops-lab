name: DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repo çek
        uses: actions/checkout@v4

      - name: Dosyayı göster
        run: cat app.txt

      - name: Pipeline çalıştı mesajı
        run: echo "DevOps pipeline başarıyla çalıştı 🚀"
